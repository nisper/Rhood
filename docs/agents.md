# Инструкции по проекту

Это high-fidelity прототип, а не production.

Когда ссылаешься на локальные документационные файлы, по возможности указывай и файл, и название нужной секции. Номера строк необязательны и должны восприниматься только как примерный ориентир, а не как основной способ навигации.

Обращайся к пользователю на "ты".

Отвечай коротко: сначала главный вывод, потом 3–5 пунктов максимум. Без длинных объяснений, если пользователь не просит.

В работе над инструкциями не пиши финальный текст инструкции целиком, пока пользователь не попросит. Сначала предлагай структуру и 2–3 ключевых правила.

Диапазоны, интервалы и иные промежутки отделяй знаком `–`, а не знаком `-`. Пример: `10–20`, `май–июнь`, `стр. 5–7`.

Используй существующие компоненты из `src/components/ui`, прежде чем создавать новый UI.

Не воссоздавай Figma-слои буквально.
Не генерируй слишком глубоко вложенную div-структуру.
Предпочитай семантический HTML.

Используй Tailwind для стилизации.
Используй shadcn/ui как основу для компонентов.
Используй lucide-react для иконок, если нет кастомной иконки.

- Если пользователь просит настроить `hover`, также добавляй `cursor-pointer`, если элемент не disabled.

# Реализация компонента

## Триггер

Когда пользователь говорит:
"Implement this component: <Figma link>"

## Инструкции

Всегда читай и соблюдай эти файлы ПЕРЕД реализацией:

- docs/instructions/implementing-a-component.md
- docs/tokens.md
- docs/token-mapping.md
- docs/component-map.md

Если инструкции отсутствуют или непонятны:

- задай уточняющий вопрос
- ИЛИ опирайся на лучшие практики реализации компонентов

# Обновление компонента из Figma

## Триггер

Когда пользователь говорит:
"Update this component: <Figma link>"

## Инструкции

Всегда читай и соблюдай эти файлы ПЕРЕД реализацией:

- docs/instructions/updating-a-component.md
- docs/tokens.md
- docs/token-mapping.md
- docs/component-map.md

Если инструкции отсутствуют или непонятны:

- задай уточняющий вопрос
- ИЛИ опирайся на правила реализации компонентов

# Реализация Figma-экрана

## Триггер

Когда пользователь говорит:
"Implement this screen: <Figma link>"

## Инструкции

Всегда читай и соблюдай эти файлы ПЕРЕД реализацией:

- docs/instructions/implementing-a-figma-frame.md
- docs/component-map.md
- docs/tokens.md

Если инструкции отсутствуют или непонятны:

- опирайся на правила реализации компонентов

# Продуктовое дизайн-мышление

## Триггер

Когда задача связана с:

- улучшением UX
- ревью пользовательских flow
- предложением решений
- продуктовыми решениями

## Инструкции

Всегда читай и соблюдай:

- docs/instructions/product-design-reasoning.md

# User Flow

## Триггер

Когда задача связана с:

- рисованием user flow
- доработкой user flow
- ревью user flow
- описанием сценария пользователя в виде flow

## Инструкции

Всегда читай и соблюдай:

- docs/instructions/drawing-a-user-flow.md

# Работа в Figma

## Триггер

Когда задача связана с:

- созданием нового макета в Figma
- обновлением существующего макета в Figma
- исследованием UI-концепта прямо в Figma
- адаптацией экрана под новую продуктовую сущность, группировку или режим просмотра

## Инструкции

Всегда читай и соблюдай:

- docs/instructions/working-in-figma.md

## Переключение режима

Если задача звучит так:

- "Implement component"
- "Refactor code"
- "Build screen"

→ приоритизируй выполнение и следуй правилам реализации компонентов

Если задача звучит так:

- "Improve UX"
- "Review flow"
- "Propose solution"

→ применяй продуктовое дизайн-мышление

# Заполнение данными макетов

Общие правила заполнения макетов данными описаны в `docs/instructions/working-in-figma.md`.

## Выдача

Если я прошу заполнить выдачу данными и даю ссылку на макет:

- Проанализируй макет;
- Возьми данные из src/data/mock/apartment-listings.ts;
- Сопоставь данные с макетом и заполни макет.
